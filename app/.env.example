# StreamWatch PostgreSQL — copy to .env.local and fill with real values.
# Do not commit .env.local. Get credentials from team / streamwatch-data repo.

DB_HOST=
DB_PORT=5432
DB_NAME=
DB_USER=streamwatch_readonly
DB_PASSWORD=
DB_SSLMODE=require
